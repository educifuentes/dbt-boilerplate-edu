# Streamlit secrets file
# IMPORTANT: Never commit this file to version control

# Supabase credentials (for Supabase Python client)
SUPABASE_URL = "https://dummy-url.supabase.co"
SUPABASE_KEY = "dummy-key"
# Direct PostgreSQL connection (optional, for direct database access)
DATABASE_URL = "postgresql://USER:PASSWORD@HOST:5432/postgres"

# List of emails allowed to access the app
allowed_emails = ["dummy@email.com", "another@email.com"]

[auth]
redirect_uri = "https://dummy-app.onrender.com/"
cookie_secret = "dummy-cookie-secret" 
client_id = "dummy-client-id.apps.googleusercontent.com"
client_secret = "dummy-client-secret"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

[gcp_cloud_sql]
INSTANCE_CONNECTION_NAME = "project:region:instance"
DB_USER = "dummy-user"
DB_PASS = "dummy-password"
DB_NAME = "dummy-db"
TABLE_NAME = "transactions"
CSV_FILE = "seeds/uploads/fct_transactions.csv"
